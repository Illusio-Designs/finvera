# Nixpacks configuration for Railway
# This file should be in the backend/ directory if Railway is configured to use backend/ as root

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install --production"]

[start]
cmd = "npm start"

